{
  "name": "elisa-pilot-dilution-planner",
  "version": "0.4.1",
  "private": true,
  "description": "Browser-only public beta for transparent ELISA pilot dilution coverage planning.",
  "scripts": {
    "build": "node scripts/build-single-file.js",
    "test": "node --test tests/*.test.js",
    "check": "npm run build && npm test && node --check src/planner.js && node --check src/i18n.js && node --check src/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
