{
  "name": "elisa-pilot-dilution-planner",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only prototype for transparent ELISA pilot dilution coverage planning.",
  "scripts": {
    "build": "node scripts/build-single-file.js",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build && node --check src/planner.js && node --check src/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
